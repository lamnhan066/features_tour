/// Represents the different button types available in the pre-dialog.
enum PreDialogButtonType {
  /// The accept button, which is typically used to confirm an action.
  accept,

  /// The later button, which allows the user to postpone a decision.
  later,

  /// The dismiss button, which is used to close the dialog without taking action.
  dismiss,
}
