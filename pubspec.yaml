name: features_tour
description: Features Tour is a package that enables you to easily create tours to introduce your widget features in your app with order.
version: 0.2.0
homepage: https://github.com/vnniz/features_tour

screenshots:
  - description: "Introduction"
    path: assets/intro.webp

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
